export type Message = {
    sender: number,
    time: string,
    header: boolean,
    src?: string,
    text?: string
};

export type Person = {
    name: string,
    class?: string,
    src?: string
};


